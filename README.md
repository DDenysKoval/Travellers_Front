# Travellers_Front

Front-end for the Travellers project — user-facing web interface.

## 📝 Description

Travellers_Front is the client side (front-end) of the Travellers web application.  
It is implemented using a modern stack (Next.js / React / TypeScript / CSS or a similar styling approach) and is responsible for rendering the user interface, handling routing, interacting with the API/backend, and managing state/data within the UI.

## 🚀 Demo

[https://travellers-front.vercel.app](https://travellers-front.vercel.app) — live demo of the front-end application (if deployed).

## 🧰 Technologies / Stack

- Next.js  
- React  
- TypeScript  
- CSS-modules 
- Other dependencies — see `package.json`
